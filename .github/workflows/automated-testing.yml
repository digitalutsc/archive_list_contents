name: Automated testing

on: [push]

jobs:
  call-testing-workflow:
    uses: digitalutsc/reusable_workflows/.github/workflows/ci-testing.yml@main
    with:
      module_name: archive_list_contents
      composer_name: drupal/archive_list_contents
      test_suite: null
